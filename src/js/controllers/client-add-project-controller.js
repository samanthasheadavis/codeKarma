angular.module('codeKarmaApp').controller('ClientAddProjectController', function($state) {
  this.message = "in ClientAddProjectController";

  // collect form data and put into project object

  // post project object to backend

  
});
